# 2026-04-13 M1 脚手架实施

## 概要

从零开始实施 M1 milestone（项目脚手架 + CI + 基础设施），18 个 commit，111 个文件，6311 行代码。分支 `feat/m1-scaffolding`。

## 完成内容

### 后端（6 层 Maven multi-module）
- **server/pom.xml**: Spring Boot 3.5.3 parent + flatten-maven-plugin + BOM
- **mb-common**: 7 security 接口/记录 + 7 exception 类 + SnowflakeIdGenerator + PageQuery/PageResult（零 Spring 依赖）
- **mb-schema**: Flyway V1（mb_iam_user 表）+ jOOQ codegen profile（gmaven-plus 方案）+ 生成代码入库
- **mb-infra**: 11 子模块（4 个 M1 实现 + 7 占位）
  - infra-jooq: JooqHelper + SlowQueryListener + ID 生成器 Bean + AutoConfiguration
  - infra-async: AsyncConfig（4/8/200 线程池 + CallerRunsPolicy）
  - infra-observability: TraceIdFilter + Actuator
  - infra-archunit: 3 条 M1 规则（jOOQ 隔离 + 模块边界 + 无循环依赖）
- **mb-platform + mb-business**: 占位 pom
- **mb-admin**: Spring Boot 入口 + ClockConfig（ADR-0012）+ 全量 application.yml + logback + BaseIntegrationTest + ArchitectureTest
- **mvn verify**: 18 模块 BUILD SUCCESS，4 tests 全绿

### 前端（5 层 pnpm workspace）
- 6 包骨架（ui-tokens / ui-primitives / ui-patterns / app-shell / api-sdk / web-admin）
- L1 默认主题：46 语义 token（oklch 颜色，Tailwind v4 @theme）
- 质量门禁：biome + stylelint + dependency-cruiser + env 一致性脚本
- Mock 登录页（验证主题 token 端到端生效）
- pnpm check:types + lint 全绿，Vite dev server 176ms 启动

### 基础设施
- docker-compose.yml（PG16 + Redis）
- scripts/dev.sh
- .github/workflows/server.yml + client.yml

## Review 修复

全量 Spec + Code Review 后修复 2 Critical + 5 Important + 2 Minor：
- DataScopeType 枚举对齐 spec（4 值 → 5 值）
- CurrentUser 接口补齐 roles()/hasRole()/isAdmin()
- @Validated + 参数校验加到 ConfigurationProperties
- 新增 BypassDataScope 注解、application-prod.yml
- AsyncConfig 改为 @AutoConfiguration
- DataScope 补齐 all()/self() 工厂方法
- CI 补齐 stylelint + dependency-cruiser 检查

## 踩坑记录

| 问题 | 原因 | 解决 |
|------|------|------|
| `spring-boot:run -pl mb-admin` 找不到 parent | `${revision}` 未被 flatten | 加 flatten-maven-plugin |
| Testcontainers 1.20.4 连不上 Docker | Docker Desktop 4.59 API 不兼容 | 升级到 1.21.4 |
| jOOQ codegen 两个插件无法共享容器 | 不同 classloader | 改用 gmaven-plus 统一管理 |
| ArchUnit 规则空匹配失败 | M1 没有 domain/web 代码 | 加 allowEmptyShould(true) |
| 本机 PG 占 5432 端口 | 和 Docker PG 冲突 | 在本机 PG 建 metabuild 用户 |

## 遗留事项（明早决策）

1. **分支合并策略**：`feat/m1-scaffolding` → `main` 是 squash merge 还是保留 18 个 commit？
2. **CLAUDE.md 更新**：M1 状态标记为"已完成"需要在 merge 后更新
3. **Review 报告中 8 个 Minor 项**：M3（ArchitectureTest 包路径）、M1（SessionData tenantId 类型）等细节是否要修
4. **本机 PG 端口冲突**：docker-compose 要不要改默认端口为 15432？还是保持 5432（新用户开箱即用）？

## 新增 Rules（5 条）

- `maven-ci-friendly-versions.md` — ${revision} 必须配 flatten-maven-plugin
- `testcontainers-version-compat.md` — Testcontainers 版本与 Docker Desktop 兼容性
- `jooq-codegen-testcontainers.md` — jOOQ codegen + Flyway + Testcontainers 的正确集成方式
- `archunit-empty-rules.md` — ArchUnit 空规则匹配 + @ArchTest vs @Test
- `local-pg-port-conflict.md` — 本机 PG 与 Docker PG 端口冲突
- `parallel-subagent.md` — 前后端隔离时可以并行派 subagent
