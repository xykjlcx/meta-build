/** 公告新增/编辑抽屉 — Task 6 完整实现 */
export function NoticeDrawer(_props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  noticeId: number | null;
  onSuccess: () => void;
}) {
  return null;
}
